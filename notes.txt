sudo tcpdump -i en1 -s 0 -A port 80 and host betabong.kicks-ass.net

sudo tcpdump -w freitag.tcpdump -i en0 -p -s 0 port 80
tcpdump -t freitag.tcpdump -A

prefs barcode: 111033



--------------

extension for camera capture skipping mode

- server config xml additional flag
  xpath /xml/options/nocapture

- 

--------------


crop

in dem verhältnis: 515 x 412
ok. sie schneiden also den maximalen ausschnit mit diesem seitenverhältnis genau aus der mitte der von der kamera gelieferten bilder?

